<Window x:Class="Task10.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task10"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:Task10.ViewModels"
        Title="MainWindow" Height="220" Width="300">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <local:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
    </Window.Resources>
    <StackPanel>
        <TextBlock Text="Форма входа" HorizontalAlignment="Center" Margin="10" FontWeight="ExtraBold" FontSize="14"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Content="Логин: " VerticalAlignment="Center"/>
            <Label Grid.Row="1" Content="Пароль: " VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Margin="5" Padding="2" 
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, Delay=100}"/>
            <TextBox Grid.Column="1" Grid.Row="1" Margin="5" Padding="2" 
                     Text="{Binding Password, UpdateSourceTrigger=PropertyChanged, Delay=100}"/>
        </Grid>
        <Button Content="Войти" Width="100" Margin="5" Padding="2"
                Command="{Binding LoginCommand}"/>
        <Grid>
            <Border VerticalAlignment="Center" HorizontalAlignment="Center" Width="250" Height="30" Margin="5" CornerRadius="5" BorderThickness="1" BorderBrush="Gray" 
                    Background="{Binding IsSuccess, Converter={StaticResource BoolToBrushConverter}}"/>
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" 
                       Text="{Binding StatusMessage}" Foreground="White" FontWeight="DemiBold"/>
        </Grid>

    </StackPanel>
</Window>
