<Window x:Class="FamalyPlacment.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FamalyPlacment.Views"
        xmlns:viewModels="clr-namespace:FamalyPlacment.ViewModels" 
        d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
        Title="Размещение растений"
    Width="400"
    Height="300"
    MinWidth="350"
    MinHeight="250"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Тип мебели  -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label
                Grid.Column="0"
                Margin="0,0,10,0"
                VerticalAlignment="Center"
                Content="Тип растений:" />
            <ComboBox
                Grid.Column="1"
                ItemsSource="{Binding PlantTypes}"
                SelectedItem="{Binding SelectedPlantType}">
                <ComboBox.Resources>
                    <viewModels:PlantTypeConverter x:Key="PlantTypeConverter" />
                </ComboBox.Resources>
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource PlantTypeConverter}}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <!--  Количество  -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label
                Grid.Column="0"
                Margin="0,0,10,0"
                VerticalAlignment="Center"
                Content="Количество:" />
            <TextBox
                Grid.Column="1"
                Padding="5,2"
                VerticalContentAlignment="Center"
                Text="{Binding Count, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <!--  Статус  -->
        <TextBlock
            Grid.Row="2"
            Margin="0,0,0,15"
            FontStyle="Italic"
            Foreground="Gray"
            Text="{Binding StatusMessage}"
            TextWrapping="Wrap" />

        <!--  Кнопка размещения  -->
        <Button
            Grid.Row="4"
            Padding="20,8"
            Command="{Binding PlaceCommand}"
            Content="Разместить"
            FontSize="14"
            FontWeight="Bold" />
    </Grid>
</Window>